import { LoginForm } from '@/components/auth/login-form';

export default function LoginPage() {
  return (
    <main className="flex min-h-[80svh] items-center justify-center p-6">
      <LoginForm />
    </main>
  );
}
